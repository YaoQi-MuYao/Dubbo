<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://dubbo.apache.org/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                           http://www.springframework.org/schema/beans/spring-beans-4.3.xsd
                           http://dubbo.apache.org/schema/dubbo
                           http://dubbo.apache.org/schema/dubbo/dubbo.xsd">

    <!-- 提供方应用信息，用于计算依赖关系 -->
    <!-- 指定当前服务。以用的名称（同样的服务名字相同，不要和别的服务名字相同 -->
    <dubbo:application name="user-service-provider"  />

    <!-- 使用multicast广播注册中心暴露服务地址 -->
    <!-- 指定注册中心 -->
    <dubbo:registry address="zookeeper://120.79.191.168:2181" />

    <!-- 用dubbo协议在20880端口暴露服务 -->
    <!-- 指定服务之间通信协议以及端口号 -->
    <dubbo:protocol name="dubbo" port="20880" />

    <!-- 声明需要暴露的服务接口 -->
    <dubbo:service interface="com.wenruo.service.UserService" ref="userServiceImpl" />

    <bean id="userServiceImpl" class="com.wenruo.service.impl.UserServiceImpl"/>


</beans>